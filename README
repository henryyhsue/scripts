#link the plist to the user directory for agents
ln -s daily_timestamps.plist ~/Library/LaunchAgents/daily_timestamps.plist

# load that plist above
$ launchctl load daily_timestamps.plist

# make sure it's loaded
$ launchctl list | grep daily
-       0       daily_timestamps.sh
